language: node_js
node_js:
  - node
  - 10
env:
  - TYPE=first
  - TYPE=second
job:
  exclude:
    - node_js: "node"
      env: TYPE=second
install: npm install
script:
  - npm run test:$TYPE
  - npm run build:$TYPE
  - sh ./build.sh $TYPE